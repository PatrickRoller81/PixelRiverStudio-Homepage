{
  "name": "pixelriverstudio-homepage",
  "version": "1.0.0",
  "description": "PixelRiverStudio Homepage & DevLog",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "test": "npx @11ty/eleventy && node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
